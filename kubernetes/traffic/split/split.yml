apiVersion: split.smi-spec.io/v1alpha2
kind: TrafficSplit
metadata:
  name: api-split
  namespace: osm-sample-app
spec:
  service: api.osm-sample-app # <root-service>.<namespace>
  backends:
    - service: api-v1
      weight: 80
    - service: api-v2
      weight: 20
